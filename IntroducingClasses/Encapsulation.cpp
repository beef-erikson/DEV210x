// Example of Encapsulation
// Created by Beef Erikson Studios on 9/29/2019.
//

#include "Encapsulation.h"
#include <iostream>

using namespace std;

/* Uncomment to use - here for build purposes

int main()
{
    // normally we would call this Rectangle, but for the sake of demo, named this.
    // see Encapsulation.h for method / constructor definitions
    Encapsulation rect { 3, 3 };
    int theArea{rect.get_area()};
    cout << theArea << endl;

    rect.resize(5, 32);
    cout << rect.get_area() << endl;

    Encapsulation defaultRect{};
    cout << "Width: " << defaultRect.get_width() << " Height: " << defaultRect.get_height() << endl;
    cout << "Area of default rectangle: " << defaultRect.get_area() << endl;

    return 0;
}

*/